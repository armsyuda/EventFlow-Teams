# 이벤트 체크리스트 사용 안내서

## 실행

바탕 화면 또는 시작 메뉴의 `이벤트 플로우` 바로가기를 더블클릭합니다. Python을 별도로 설치할 필요가 없습니다.

처음 받은 배포본을 실행하면 `%LOCALAPPDATA%\Programs\EventFlow`에 고정 설치되고 바로가기가 만들어집니다.
사용자 데이터 폴더에는 데이터베이스와 자동 백업 폴더가 만들어집니다.

## 앱 업데이트

- 앱을 시작하면 공개 GitHub Release의 새 버전을 자동으로 확인합니다.
- 상단바에는 현재 설치 버전, GitHub 공개 버전과 공개 날짜가 함께 표시됩니다.
- 새 버전이 있으면 상단바의 `업데이트` 버튼이 활성화됩니다.
- 새 버전이 없으면 `다시 확인`을 눌러 언제든 공개 릴리스를 다시 조회할 수 있습니다.
- 버튼을 누르면 업데이트를 내려받고 앱을 종료한 뒤 고정 설치 폴더를 자동 교체하여 다시 실행합니다.
- 행사 데이터와 백업은 설치 폴더 밖에 있으므로 업데이트해도 유지됩니다.
- 새 버전이 정상적으로 시작되지 않으면 이전 버전으로 자동 복구합니다.
- 업데이트가 실패하면 기존 버전을 자동으로 다시 실행하고 `%LOCALAPPDATA%\EventCheckList\updates`에 버전별 로그를 남깁니다.
- 업데이트 확인이 실패하면 안내창에 `확인된 원인`, `확인 방법`, `오류 상세`가 표시됩니다. 인터넷·DNS·시간 초과·GitHub 공개 설정·ZIP 누락·저장 권한·파일 손상을 서로 구분해 안내합니다.
- 안내창을 닫은 뒤에도 상단의 `다시 확인` 버튼에 마우스를 올리면 마지막 실패 원인을 다시 볼 수 있습니다.

## 첫 행사 만들기

1. 대시보드 또는 행사 화면에서 `+ 새 행사`를 누릅니다.
2. 행사명과 `준비 시작일`, `최종 행사일`을 입력합니다.
3. 이 행사를 총괄하는 `PM 업체`를 선택합니다. 장소, 주최/주관, 예산은 필요한 경우 입력합니다.
4. 기본 항목 120개가 모두 선택되어 있습니다. 이번 행사에 필요 없는 분류나 항목의 체크를 해제합니다.
5. `저장`을 누르면 모든 업무가 날짜 없는 상태로 만들어집니다. 각 업무의 작업 시작일과 마감일은 체크리스트에서 직접 입력합니다.

## 체크리스트 사용

- 가장 왼쪽 체크박스를 누르면 즉시 완료 처리됩니다.
- 상태는 미착수, 진행중, 확인요청, 완료, 보류, 해당없음 중에서 선택합니다.
- 해당없음은 진행률 계산에서 제외됩니다.
- 작업 시작일과 마감일 입력칸을 클릭하면 달력이 바로 열립니다. 업무 날짜는 행사마다 직접 입력합니다.
- 마감일 다음에는 `담당자(PM) → 업체 → 업체담당자 → 업체담당자 전화번호`가 이어집니다.
- `담당자(PM)`에는 행사 정보에서 선택한 PM 업체 소속 담당자만 표시됩니다.
- `업체`를 선택하면 해당 업체 소속 사람만 `업체담당자`에 표시되고, 담당자를 선택하면 전화번호가 자동 표시됩니다.
- 설정에서 업체와 담당자를 추가한 뒤 체크리스트를 새로 열거나 새로고침하면 바로 선택할 수 있습니다.
- 모든 표의 열 경계를 드래그해 폭을 바꾸거나 열 제목을 끌어 순서를 바꿀 수 있습니다. `열 너비 맞춤`은 현재 창 크기에 맞게 열을 자동 정리합니다.

## 달력

- 날짜 칸 아래의 얇은 기간 박스는 해당 날짜에 진행 중인 개별 업무를 뜻합니다. 색상은 업무 분류와 완료 상태를 구분합니다.
- 기간 박스 안에는 업무명이 작은 글자로 표시됩니다. 미완료 업무 중 마감일이 가까운 항목이 먼저 표시됩니다.
- 한 날짜의 업무가 표시 공간보다 많으면 `+N개 더보기`가 나타납니다. 해당 날짜를 누르면 오른쪽 목록에서 생략된 업무까지 모두 확인할 수 있습니다.
- 날짜를 누르면 오른쪽 카드에 업무명, 작업기간과 상태가 표시됩니다. 날짜를 입력하지 않은 업무는 달력에 표시되지 않습니다.
- 오른쪽 카드의 `완료 처리`를 누르면 체크리스트도 즉시 완료 처리되고 달력 기간 막대는 사라집니다. `완료 취소`로 되돌릴 수 있습니다.
- 미완료 업무의 마감일이 오늘이면 카드가 `오늘 마감`으로 강조되고 `내일까지`, `모레까지`, `날짜 선택`이 나타납니다. 다른 날짜의 업무에는 연장 버튼이 표시되지 않습니다.

## 기본 항목

- 기본 항목을 비활성화하면 이후 새 행사에서 제외됩니다.
- `+ 항목 추가`로 새 기본 항목을 만들 수 있습니다.
- 대분류, 중분류, 항목명, 세부내용, 수량과 단위는 셀을 한 번 눌러 바로 수정할 수 있습니다.
- 업체와 업체담당자는 표 안의 선택 상자에서 바로 변경할 수 있습니다.
- 기본 항목에는 D-Day 기준이나 일정값이 없습니다. 실제 날짜는 행사 체크리스트에서 직접 입력합니다.
- 행을 선택하고 `선택 삭제`를 누르면 기본 항목에서 삭제됩니다. 이미 만들어진 행사의 업무는 그대로 유지됩니다.
- 기본 항목 변경은 이미 만들어진 행사에는 적용되지 않습니다.

## 기존 데이터 보존

- 기존 행사와 체크리스트의 날짜, 업체, 담당자 정보는 유지됩니다.
- 새 버전에서 필요한 PM 정보 칸만 안전하게 추가합니다.
- 데이터 구조를 변경하기 전 데이터베이스를 같은 데이터 폴더에 별도 복사합니다.

## 백업과 내보내기

- 앱은 하루 한 번 자동 백업합니다.
- 설정에서 수동 백업, 백업 복원, Excel 내보내기를 실행할 수 있습니다.
- 백업 복원은 현재 데이터를 교체하므로, 프로그램이 먼저 현재 상태를 안전 백업한 뒤 복원합니다.

## 데이터 위치

- 프로그램: `%LOCALAPPDATA%\Programs\EventFlow\EventFlow.exe`
- 데이터베이스: `%LOCALAPPDATA%\EventCheckList\data\event_checklist.db`
- 자동 백업: `%LOCALAPPDATA%\EventCheckList\backups`
